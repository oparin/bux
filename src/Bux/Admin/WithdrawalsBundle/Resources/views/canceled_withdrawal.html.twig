{% extends ':admin:admin_base.html.twig' %}

{% block content_title %}
    <i class="fa fa-arrow-circle-o-up"></i>&nbsp; Withdrawals - Canceled Withdrawal
{% endblock content_title %}

{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="box">
                <header>
                    <div class="icons">
                        <i class="fa fa-table"></i>
                    </div>
                    <h5>Canceled Withdrawal</h5>
                </header>
                <div class="body" id="collapse4">
                    {{ grid_search(grid, ':admin:admin_grid.html.twig') }}
                    {{ grid(grid, ':admin:admin_grid.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock content %}

{% block js %}
    <script type="text/javascript">
        $(document).ready(function() {
            $(function() {
                $( "#accordion" ).accordion({
                    collapsible: true,
                    active: false
                });
            });
        });
    </script>
{% endblock js %}